const express = require('express'); const router = express.Router(); router.post('/validate-session', (req, res) => { res.json({ valid: true, user: { id: 'demo', email: 'demo@proofpixapp.com', plan: 'professional' } }); }); router.post('/login', (req, res) => { res.json({ success: true, sessionToken: 'demo_token', user: { id: 'demo', email: 'demo@proofpixapp.com', plan: 'professional' } }); }); module.exports = router;
